import { DateToAgePipe } from './date-to-age.pipe';

describe('DateToAgePipe', () => {
  it('create an instance', () => {
    const pipe = new DateToAgePipe();
    expect(pipe).toBeTruthy();
  });
});
